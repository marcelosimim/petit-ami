import 'package:flutter/material.dart';
import 'package:petitami/components/AppBar.dart';

class AccountConfigurations extends StatelessWidget {
  const AccountConfigurations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Em construção'),
    );
  }
}
